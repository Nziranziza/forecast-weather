import { Typography } from '@material-ui/core';
const Footer = () => {
  return <footer>
    <div className="container">
      <Typography variant="h4">Weather App</Typography>
      <Typography variant="subtitle1">by Elias Pokemon</Typography>
    </div>
  </footer>
};

export default Footer;
